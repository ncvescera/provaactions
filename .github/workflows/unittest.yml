name: "Unittests"

on:
  push:
    branches:
      - develop
      - main

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: ./.github/workflows/unittest-core.yml

